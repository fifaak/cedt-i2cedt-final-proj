# Frontend environment variables (example)

# Port for the Express static server
PORT=3000

# Backend API proxy target for /api
BACKEND_HOST=localhost
BACKEND_PORT=3001

# Optional: force API base used by the browser client (otherwise auto-detected via query param `?api=`)
# API_BASE=http://localhost:3001/api


